import {useState} from 'react'

// Create a variable for your backend URL

const URL = 'https://my-node-app-xirq.onrender.com'

export const CreateRemindly = () => {
    // State variable to store reminder
    const [reminder, storeReminder] = useState([])
    // State variable to store loading state
    const [loading, editReminder] = useState([])
    // handleSubmit() function to send POST request
    
    // return HTML
    return (
        // container div
   <d></d>
            // header elem that says "Add reminder"

            // input elem for user to add reminder

            // button to add reminder (sends post request with onClick)
           
    )
}
